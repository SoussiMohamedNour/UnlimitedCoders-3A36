<form method="post" action="{{ path('app_facteurcrud_delete', {'id': facteur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facteur.id) }}">
    <button class="btn">Delete</button>
</form>
