{% extends 'base.html.twig' %}
{% block body %}

{% block topBar %}

   <!-- ======= Top Bar ======= -->
<div id="topbar" class="d-flex align-items-center fixed-top">
    <div class="container d-flex justify-content-between">
        <div class="contact-info d-flex align-items-center">
            <i class="bi bi-envelope"></i> <a href="mailto:healthiefield@esprit.com">healthiefield@esprit.com</a>
            <i class="bi bi-phone"></i> +216 99170951
        </div>
        <div class="d-none d-lg-flex social-links align-items-center">
            <a href="#" class="twitter"><i class="bi bi-twitter"></i></a>
            <a href="https://www.facebook.com/omar.bali23" class="facebook"><i class="bi bi-facebook"></i></a>
            <a href="#" class="instagram"><i class="bi bi-instagram"></i></a>
            <a href="#" class="linkedin"><i class="bi bi-linkedin"></i></a>
        </div>
    </div>
</div>

<!-- ======= Header ======= -->
<header id="header" class="fixed-top">
    <div class="container d-flex align-items-center">

        <h1 class="logo me-auto"><a href="index.html">Healthified</a></h1>
        <!-- Uncomment below if you prefer to use an image logo -->
        <!-- <a href="index.html" class="logo me-auto"><img src="assets/img/logo.png" alt="" class="img-fluid"></a>-->

        <nav id="navbar" class="navbar order-last order-lg-0">
            <ul>
              <li><a class="nav-link scrollto active" href="{{path('app_article_indexfront')}}">Accueil</a></li>
                <li><a class="nav-link scrollto" href="{{path('app_article_indexfront')}}">A propos</a></li>
                <li><a class="nav-link scrollto" href="{{path('app_article_indexfront')}}">Services</a></li>
                
                <li><a class="nav-link scrollto" href="{{path('app_article_indexfront')}}">Article</a></li>
               
                <li><a class="nav-link scrollto" href="{{path('app_article_indexfront')}}">Medecin</a></li>
                <li><a class="nav-link scrollto" href="{{path('app_article_indexfront')}}">Contact</a></li>
            </ul>
            <i class="bi bi-list mobile-nav-toggle"></i>
        </nav><!-- .navbar -->

        <a href="#appointment" class="appointment-btn scrollto"><span class="d-none d-md-inline">Rendez-vous</span> </a>

    </div>
</header><!-- End Header -->

 

{% endblock %}
{% block welcome %}
    <div class="blog">
    <div class="container">
    <div class="row">

    <!-- Blog Content -->

    <div class="col-lg-8">
        <script >
            /* Voici la fonction javascript qui change la propriété "display"
            pour afficher ou non le div selon que ce soit "none" ou "block". */
            function togg(){
                let d1 = document.getElementById("d1");
                if(getComputedStyle(d1).display != "none"){
                    d1.style.display = "none";
                } else {
                    d1.style.display = "block";
                }
            };
            function AfficherMasquer()
            {
                divInfo = document.getElementById('d1');

                if (divInfo.style.display == 'none')
                    divInfo.style.display = 'block';
                else
                    divInfo.style.display = 'none';

            }

        </script>
        <div class="blog_post_container">
            <div class="blog_">
                <div class="blog_post_image">
                    <img src="{{ vich_uploader_asset(article,'imageFile') }}" height="300">
                    <div class="blog_post_date d-flex flex-column align-items-center justify-content-center">
                        <div class="blog_post_day">{{ article.articleDate ? article.articleDate|date('d') : '' }}</div>
                        <div class="blog_post_month">{{ article.articleDate ? article.articleDate|date('m-y') : '' }}</div>
                    </div>
                </div>
                <div class="blog_post_title"><h1>{{ article.titre }}</h1></div>
                <div class="blog_post_text">
                    <p>{{ article.articleDesc }}</p>
                </div>
                <div class="blog_post_meta">
                    <ul>
                        <li class="blog_post_meta_item">
                            <button id="btn" style="color:#a5a5a5;border: none;background-color: #feffff" onClick="togg()">
                                {% if ( article.nbcomment ==0) %}
                                    Aucun commentaire ajouté
                                {% elseif ( article.nbcomment ==1)%}
                                    {{ article.nbcomment }} commentaire
                                    {% else %}
                                        {{ article.nbcomment }} commentaires
                                {% endif %}
                            </button>


                           <div class="blog_post_text" id="d1" style="display:none">
                               {# {% if app.user %}
                                    {% for comment in commentaires %}
                                        {% if app.user.id==comment.idclientComment.id %}
                                            <p style="color: #0a1219">Vous</p>
                                            <form method="post" action="{{ path('commentaire_delete2', {'idComment': comment.idComment}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer ce commentaire?');">
                                                <input type="hidden" name="_method" value="DELETE">
                                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment.idComment) }}">
                                                <p> {{ comment.contenuComment }} : {{ comment.datecomment ? comment.datecomment|date('Y-m-d'):''}}
                                                    <button id="btn" style="color:#a5a5a5;border: none;background-color: #feffff"> <img src="{{ asset('images/delete.png') }}" width="25" height="25"></button>
                                                </p>
                                            </form>

                                        {% else %}#}
                                           {# <p style="color: #0a1219">{{ comment.nomClient }} {{ comment.prenomClient }}</p>#}
                               {% for comment in commentaires %}
                                            <p> {{ comment.commentairecontenu }} : {{ comment.commentairedate ? comment.commentairedate|date('Y-m-d'):''}}</p>
                               {% endfor %}
                               {#  {% endif %}
                                    {% endfor %}
                                {% endif %}  #}
                            </div>


                        </li>
                    </ul>

                </div>


                {{ include('article/_formCommentaire.html.twig', {'button_label': 'Commenter'}) }}



            </div>
        </div>


       <div class="col-lg-4 sidebar_col">

            <div class="sidebar_latest_posts">
                <div class="sidebar_title">Article Récemment ajoutés</div>
                <div class="latest_posts_container">
                    <ul>
                        {% for article in articles %}
                            <!-- Latest Post -->
                            <li class="latest_post clearfix">
                                <div class="latest_post_image">
                                    <a href="{{ path('app_article_showfront', {'id': article.id}) }}"><img src="{{ vich_uploader_asset(article,'imageFile') }}" width="60" height="60"></a>
                                </div>
                                <div class="latest_post_content">
                                    <div class="latest_post_title trans_200"><a href="{{ path('app_article_showfront', {'id': article.id}) }}">{{ article.titre }}</a></div>
                                    <div class="latest_post_meta">

                                        <div class="latest_post_date trans_200">{{ article.articleDate ? article.articleDate|date('Y-m-d'):''}}</div>
                                    </div>
                                </div>
                            </li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
{% endblock %}