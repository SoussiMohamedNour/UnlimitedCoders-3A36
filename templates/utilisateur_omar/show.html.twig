{% extends 'base.html.twig' %}

{% block title %}UtilisateurOmar{% endblock %}

{% block body %}
    <h1>UtilisateurOmar</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ utilisateur_omar.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_utilisateur_omar_index') }}">back to list</a>

    <a href="{{ path('app_utilisateur_omar_edit', {'id': utilisateur_omar.id}) }}">edit</a>

    {{ include('utilisateur_omar/_delete_form.html.twig') }}
{% endblock %}
