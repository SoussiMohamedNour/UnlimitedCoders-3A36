{% extends 'base.html.twig' %}

{% block title %}Ordonnance index{% endblock %}

{% block body %}
    <h1>Ordonnance index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Reference</th>
                <th>Validite</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ordonnance in ordonnances %}
            <tr>
                <td>{{ ordonnance.reference }}</td>
                <td>{{ ordonnance.validite }}</td>
                <td>
                    <a href="{{ path('app_ordonnance_show', {'reference': ordonnance.reference}) }}">show</a>
                    <a href="{{ path('app_ordonnance_edit', {'reference': ordonnance.reference}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_ordonnance_new') }}">Create new</a>
{% endblock %}
