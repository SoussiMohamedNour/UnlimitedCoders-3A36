<form method="post" action="{{ path('app_fiche_assurancecrud_delete', {'id': fiche_assurance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fiche_assurance.id) }}">
    <button class="btn">Delete</button>
</form>
