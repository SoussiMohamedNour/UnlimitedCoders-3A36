{% extends 'BackOffice/consultation/base.html.twig' %}

{% block title %}Modifier Medicaments{% endblock %}

{% block body %}
<section class="content-main">
    <div class="card mb-4">
        <div class="content-header">
            <h2 class="content-title">Modifier Medicament</h2>
            <a href="{{ path('app_medicaments_index')}}" class="btn btn-outline-danger"> &times; Annuler</a>
        </div>
    </div>
    <div>
    </div>
    {{ include('BackOffice/medicaments/_form.html.twig', {'button_label': 'Modifier'}) }}
    <br>
    {{ include('BackOffice/medicaments/_delete_form.html.twig') }}

</section>
{% endblock %}
