{% extends 'BackOffice/consultation/base.html.twig' %}

{% block title %}Consultation{% endblock %}

{% block body %}
<section class="content-main">

<div class="card mb-4">
    <div class="content-header">
        <h2 class="content-title">Supprimer Consultation</h2>
        <a href="{{ path('app_consultation_index')}}" class="btn btn-outline-danger"> &times; Annuler</a>
    </div>
    <div class="card-body">
        <div class="card-body">
            <div class="table table-responsive">
                <table class="table table-hover">
                    <tbody>
                <tr>
                    <th>ID Consultation</th>
                    <td>{{ consultation.idconsultation }}</td>
                </tr>
                <tr>
                    <th>Matricule Medecin</th>
                    <td>{{ consultation.matriculemedecin }}</td>
                </tr>
                <tr>
                    <th>ID Patient</th>
                    <td>{{ consultation.idpatient }}</td>
                </tr>
                <tr>
                    <th>Date Consultation</th>
                    <td>{{ consultation.dateconsultation ? consultation.dateconsultation|date('Y-m-d') : '' }}</td>
                </tr>
                <tr>
                    <th>Montant</th>
                    <td>{{ consultation.montant }}</td>
                </tr>
        </tbody>
                </table>
            </div>

    {{ include('BackOffice/consultation/_delete_form.html.twig') }}
    <br>
    <a class="btn btn-primary" href="{{ path('app_consultation_index') }}">Liste</a>

    {# <a href="{{ path('app_consultation_edit', {'idconsultation': consultation.idconsultation}) }}">edit</a> #}

        {# {{ include('BackOffice/consultation/_form.html.twig') }} #}
    </div>
</div>
</section>
    {# <h1>Consultation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idconsultation</th>
                <td>{{ consultation.idconsultation }}</td>
            </tr>
            <tr>
                <th>Matriculemedecin</th>
                <td>{{ consultation.matriculemedecin }}</td>
            </tr>
            <tr>
                <th>Idpatient</th>
                <td>{{ consultation.idpatient }}</td>
            </tr>
            <tr>
                <th>Dateconsultation</th>
                <td>{{ consultation.dateconsultation ? consultation.dateconsultation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ consultation.montant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_consultation_index') }}">back to list</a>

    <a href="{{ path('app_consultation_edit', {'idconsultation': consultation.idconsultation}) }}">edit</a>

    {{ include('BackOffice/consultation/_delete_form.html.twig') }} #}
{% endblock %}
