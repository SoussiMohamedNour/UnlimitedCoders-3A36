{% extends 'BackOffice/consultation/base.html.twig' %}

{% block title %}Edit Consultation{% endblock %}

{% block body %}
<section class="content-main">
    <div class="card mb-4">
        <div class="content-header">
            <h2 class="content-title">Modifier Consultation</h2>
            <a href="{{ path('app_consultation_index')}}" class="btn btn-outline-danger"> &times; Annuler</a>
        </div>
    </div>
    <div class="card-body">

    {{ include('/BackOffice/consultation/_form.html.twig', {'button_label': 'Modifier'}) }}
    {# <a href="{{ path('app_consultation_index') }}">back to list</a> #}
    <br>
    {{ include('/BackOffice/consultation/_delete_form.html.twig') }}
    </div>
</section>
{% endblock %}
