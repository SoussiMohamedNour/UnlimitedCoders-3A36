<form method="post" action="{{ path('app_consultation_delete', {'reference': consultation.reference}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ consultation.reference) }}">
    <button class="btn btn-primary">Supprimer</button>
</form>
