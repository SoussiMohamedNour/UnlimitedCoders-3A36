<form method="post" action="{{ path('app_ordonnance_delete', {'reference': ordonnance.reference}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
<br>
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ordonnance.reference) }}">
    <button class="btn btn-primary">Supprimer</button>
</form>
