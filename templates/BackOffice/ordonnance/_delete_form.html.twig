<form method="post" action="{{ path('app_ordonnance_delete', {'referenceordonnance': ordonnance.referenceordonnance}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ordonnance.referenceordonnance) }}">
    <button class="btn btn-primary">Supprimer</button>
</form>
