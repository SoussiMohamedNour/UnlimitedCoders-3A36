{% extends 'BackOffice/ordonnance/base.html.twig' %}

{% block body %}
<section class="content-main">
<div class="card mb-4">
    <div class="content-header">
        <h2 class="content-title">Ajouter Ordonnance</h2>
        <a href="{{ path('app_consultation_index')}}" class="btn btn-outline-danger"> &times; Annuler</a>
    </div>
    <div class="card-body">
        {{ include('BackOffice/ordonnance/_form.html.twig') }}
        <br>
        <a class="btn btn-primary" href="{{ path('app_ordonnance_index') }}">Liste</a>
    </div>
</div>
</section>
{% endblock %}
