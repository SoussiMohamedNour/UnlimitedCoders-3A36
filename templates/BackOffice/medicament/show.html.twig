{% extends 'BackOffice/consultation/base.html.twig' %}

{% block title %}Medicament{% endblock %}

{% block body %}
<section class="content-main">

    <h1>Supprimer Medicament</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medicament.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ medicament.nom }}</td>
            </tr>
            <tr>
                <th>Dosage</th>
                <td>{{ medicament.dosage }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ medicament.prix }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ medicament.description }}</td>
            </tr>
        </tbody>
    </table>

    {# <a href="{{ path('app_medicament_index') }}">back to list</a> #}

    {# <a href="{{ path('app_medicament_edit', {'id': medicament.id}) }}">edit</a> #}

    {{ include('BackOffice/medicament/_delete_form.html.twig') }}
</section>
{% endblock %}
